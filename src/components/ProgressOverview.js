import React from "react";
import { Paper, Typography } from "@mui/material";

const ProgressOverview = () => {
  return (
    <Paper sx={{ p: 2 }}>
      <Typography variant="body1">Weekly progress chart will go here.</Typography>
    </Paper>
  );
};

export default ProgressOverview;
